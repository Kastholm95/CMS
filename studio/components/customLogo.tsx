import React from 'react';
import logo from '../public/img/logo.png';

const LogoComponent = () => (
  <img src={logo} alt="Nyhedsmedia Logo" style={{ width: 'auto', height: '100%' }} />
);

export default LogoComponent;
